[project]
name = "slit-illumination"
version = "0.1.0"
description = "The instrument slit illumination model"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.9.2",
    "numpy>=2.1.2",
]
